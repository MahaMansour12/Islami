import 'package:flutter/material.dart';

class Hadeth_Tap extends StatelessWidget {
  const Hadeth_Tap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
